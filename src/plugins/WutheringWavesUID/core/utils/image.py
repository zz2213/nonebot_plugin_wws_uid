from PIL import Image, ImageDraw, ImageFont
from pathlib import Path
from ...core.resource.manager import resource_manager

async def create_user_card(user_data: dict) -> Path:
    """创建用户信息卡片"""
    # 这里实现图片生成逻辑
    # 返回图片路径
    pass